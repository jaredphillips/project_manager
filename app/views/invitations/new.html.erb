<div class="center hero-unit">
	<h2>Invite someone to join your team</h2>
	<%= form_for @invitation do |f| %>
	<%= render 'layouts/invite_error_messages' %>
	  <div class="field"> 
			<%= f.label :email, "Email" %>
			<%= f.text_field :email %></br>
		</div>

	  <div class="action"> 
			<%= f.submit "Send Invite!", class: "btn" %>
		</div>
	<% end %>
</div>
